This is a single static site in `./index.html`. Read the file at the start of every conversation.
Always use the `edit` tool, do not rewrite the whole file.
Keep all plans minimal and to the point as a numbered list with no code. Plans should not include implementation details, only logic and data flow.
Do not add unnecessary helpers, unless deduplicating logic and variables that should be centralised for ease of change.
